import styled from 'styled-components';

const SearchList = styled.div`
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  grid-gap: 1rem;
  width: 100%;
  margin-top: 1rem;
`;

export { SearchList };
